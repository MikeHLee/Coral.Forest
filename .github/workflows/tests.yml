name: tests

on:
  push:
  pull_request:

jobs:
  python:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.10", "3.11", "3.12"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - run: pip install -e ".[dev]"
      - run: pytest -q

  atlas:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      # EcoAnalyst 3.0 is not on PyPI yet; switch this to `.[dev,atlas]` once it is.
      - run: pip install -e ".[dev]" matplotlib "ecoanalyst @ git+https://github.com/MikeHLee/EcoAnalyst@main"
      - run: pytest -q tests/test_atlas.py

  r-reproduction:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: r-lib/actions/setup-r@v2
      - run: Rscript -e 'install.packages("randomForest", repos = "https://cloud.r-project.org")'
      - run: Rscript R/reproduce_2018.R
      - name: Check row counts
        run: |
          grep -q '"rows_clean": 9111, "positives": 255' results/model_a_r.json
